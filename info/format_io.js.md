**Input:** Three arguments. A matrix as an array of arrays with integers,
    first and second cell coordinates as arrays of two integers. 

**Output:** The existence of a path between two given cells as a boolean.
